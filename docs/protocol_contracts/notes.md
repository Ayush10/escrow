# Protocol Contracts Notes

- The repo already supports fallback ABI behavior in the protocol client. That is useful now, but the productization path needs one declared V1 ABI.
- `PRODUCT_PLAN.md` already argues for ABI freeze and a canonical deploy story; this lane turns that into executable repo work.
